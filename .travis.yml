language: generic
matrix:
  include:
    - os: linux
      dist: trusty
      sudo: required
    - os: osx
      osx_image: xcode7.3
install:
  - make install-deps
script:
  - make && sudo make install
