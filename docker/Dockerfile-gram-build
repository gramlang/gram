FROM gramlang/gram:deps
COPY . /root/gram
RUN cd /root && \

  # Build and lint Gram.
  cd gram && \
  rm -rf build && \
  mv ../build build && \
  touch build/release/llvm/dist/bin/llvm-config && \
  make && \
  make lint
